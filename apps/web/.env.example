# Frontend env — copy to apps/web/.env.local for local dev,
# or set these in the Vercel project's Environment Variables.

# Where the FastAPI backend lives (Railway URL in prod, localhost in dev)
NEXT_PUBLIC_API_URL=http://localhost:8000

# Clerk (added when auth is wired in)
NEXT_PUBLIC_CLERK_PUBLISHABLE_KEY=
